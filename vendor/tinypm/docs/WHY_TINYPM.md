# Why TinyPM exists

Different Linux systems use different package managers and app sources. TinyPM provides one small command style across native package managers, Flatpak, Snap, and Seed.

Seed exists so TinyPM still has a usable built-in store even when no native package manager is detected.
